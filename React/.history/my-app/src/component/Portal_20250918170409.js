import React from 'react';

function createPortalWrapper(){
    const element = document
}

const Portal = () => {
    return (
        <div>
            
        </div>
    );
};

export default Portal;