import React, { useState } from 'react';

const Timer = () => {
    const [message, setMessage] = useState('Hello Timer');
    return (
        <div>
            Timer
        </div>
    );
};

export default Timer;