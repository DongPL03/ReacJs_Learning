import React from 'react';
import { useGallery } from '../../context/galleryContext';

const PhotoList = () => {
    const {} = useGallery();
    return (
        <div>
            
        </div>
    );
};

export default PhotoList;