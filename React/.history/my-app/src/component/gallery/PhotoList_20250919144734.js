import React from 'react';
import { useGallery } from '../../context/galleryContext';

const PhotoList = () => {
    const {pho} = useGallery();
    return (
        <div>
            
        </div>
    );
};

export default PhotoList;