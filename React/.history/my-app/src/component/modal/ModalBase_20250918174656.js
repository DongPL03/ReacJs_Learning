import React from 'react';
import Portal from '../Portal';

const ModalBase = ({visible, onClose}) => {
    return (
        <>
         <Portal></Portal>   
        </>
    );
};

export default ModalBase;